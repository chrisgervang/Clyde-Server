//GET to return to the server a JSON of devices that are currently "online" according to a local server record.
//Starts with firebase list of devices and searches for the devices, updates the firebase and returns the result.

var devices = function(request, reply) {
	
}

module.exports = devices;